var express = require('express');
var router = express.Router();

/* GET users listing. */
router.get('/string', function(req, res, next) {
  res.send('Not yet implemented: /string');
});

module.exports = router;
