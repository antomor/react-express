var express = require('express');
var router = express.Router();

/* GET users listing. */
router.get('/char', function(req, res, next) {
  res.send('Not yet implemented: /char');
});

module.exports = router;
